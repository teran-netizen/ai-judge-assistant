# AI Judge: detect abuse of expensive endpoints (generate, refine)
[Definition]
failregex = ^<HOST> .* "POST /api/cases/.*/generate.*" (429|402)
            ^<HOST> .* "POST /api/cases/.*/refine.*" (429|402)
ignoreregex =
