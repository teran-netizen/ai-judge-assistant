# ═══════════════════════════════════════════════════════════════
# AI Judge — dev dependencies (linting, testing, security)
# pip install -r requirements-dev.txt
# ═══════════════════════════════════════════════════════════════

# Lint + format
ruff>=0.9.0

# Type checking
mypy>=1.14.0
types-redis>=4.6.0
sqlalchemy[mypy]>=2.0.36

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
httpx>=0.28.0

# Security
pip-audit>=2.7.0

# Pre-commit
pre-commit>=4.0.0
